name: line_chart
description: A simple flutter package to create a custom line chart
version: 1.1.11
repository: https://github.com/lucas-sesti/line_chart

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
